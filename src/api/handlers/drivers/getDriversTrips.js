const { Drivers, Trips } = require("../../../models");

module.exports.getDrivers = async (req, res) => {
  // const { _id } = req.params;
  // let { isActive } = req.query;
  // const query = {};
  // const driver = await Drivers.findOne({ _id: _id });
  // if (!driver) {
  //   return res.status(400).send({ message: "Driver was not found" });
  // }
  // if (isActive == true) {
  // }
  // const allTrips = await Trips.find({ _id: { $in: driver.tripIds } });
  // return res.status(200).send(docs);
};
